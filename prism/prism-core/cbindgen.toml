language           = "C"
include_guard       = "PRISM_H"
pragma_once         = true
autogen_warning     = "/* Généré par cbindgen — ne pas modifier à la main. */"
documentation       = true
documentation_style = "doxy"

[export]
prefix  = ""
include = ["PrSession"]

[parse]
parse_deps = false
